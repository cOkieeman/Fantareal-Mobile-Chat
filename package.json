{
  "name": "fantareal-mobile-chat",
  "version": "0.6.0-dev.1",
  "private": true,
  "type": "module",
  "description": "Fantareal Mobile Chat extension and per-card JSON-RPC service",
  "scripts": {
    "check": "node --check web/generated-app.js && node --check web/light-apps.js && node --check web/social-apps.js && node --check web/mail-apps.js && node --check web/mc6-apps.js && node --check web/app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
