{
  "name": "fantareal-mobile-chat",
  "version": "0.4.0-dev.1",
  "private": true,
  "type": "module",
  "description": "Fantareal Mobile Chat extension and per-card JSON-RPC service",
  "scripts": {
    "check": "node --check web/app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
